syntax = "proto3";
option go_package="/protobuf";
//one server, many clients....
message ClientMSG {
    bytes chunk = 1;
    string name = 2;
}
message ServerRES{
    ClientMSG msg = 1;
    int32 id = 2;    
}

service Calling {
    rpc VoIP (stream ClientMSG) returns (stream ServerRES);
}
